mvc

organiser son code ou répondre problématique en étant organisée 
Design pattern ( pleins de différents )
design patter strategie ,observer , factory , etc
Comment faire en sorte que ma class soit instancier une seule fois .
principe Solid ( single responsabilité open libre lifini etc.. )

MVC touche pas au code - 

Imposé de faire du découpage correct (pas dupliquer le code) application robust propre maintenable propre 

3 dossiers M - V - C

dossier model parfois appeler "entities"

3 dossiers obligatoire pour respecter le mvc
dossier enfants et cousins 
(pour le web dossier public (point d'entré html, asset, image))
frameworks

Models : tout le code qui sert à la manipulation des données stockées
(TinyDB  - bdd)  marche aussi pour donné stocké dans fichier

views : tout ce qui va permettre de générer et gérer l'affichage (web ex html)
Affichage message cli input user ou autres 

controllers : tout ce qui n'est ni model ni views (def debutant) à ne pas prendre au premier degrés 

views : quel âge avez vous ? dans cli
Controller : qui va gérer ça et faire quelque chose en conséquence retourne quelque chose à la views
Controller : Il va dire j'ai besoin donnés users, faire appel au code modèle, pour traiter les donner recu, il va traiter les prénoms et les retourner à la vue (médiation / passerel entre views et models)

( Priorité doit marcher sur les différents systems exploitations )
Aucune fonction + 50l

Découpage model en deux instances
Entités = représentation sous forme de code d'un objet complexe (objet complexe 
aquarium objet complexe ce n'est pas chaine de carac, boolean, tableau. Aquarium fait de contenance dimension, entier, chaine, hauteur, type d'eau eau de mer eau douce, etc... entité object complexe qui se base eux même sur des objets simple)
avec bdd représentation sous forme de code avec relationel ou non relationel ( représentation des tâbles et donnés )

Manager = code qui va permettre de manipuler la base de donnée ( insert poisson dans aquarium ) dans poisson manager, insert prendre donnée dans l'instance entité et inserer en bdd. select, update, delete.

tournois, des tours, des matchs et des joueurs.

Etape 1 entité def attribut et après relation

poo privé que la class qui peut modfier l'attribut
public all 
protectid ( seul la class et ses enfants ) 
python pas de visibilité.
Quand quelque chose est privé on met un underscore devant 

A faire : 

Finir les entities

dans def main ( faire le menu de quesque vous voulez faire, ex faire un tournois 
 combien de joueur, menu se ré affiche
menu )

Ensuite essayé de faire dans views dossier tournois dans dossier mkdir create.py  dedans ( question sur comment créer un tournoi avec input utilisateurs ( nom du tournoi ))








from controllers.PlayerController import PlayerController
from controllers.TournoisController import TournoisController


menu_options = {
    1: '➕ 🧞‍♀️ Nouveau Joueur',
    2: '📋Listes des joueurs',
    3: '➕♟️ Nouveau Tournois♟ ♟️',
    4: '📋Listes des tournois',
    5: 'Exit',
}

def main():
  for key in menu_options.keys():
    print (key, '--', menu_options[key] )

if __name__=='__main__':
    while(True):
        main()
        option = ''
        try:
            option = int(input('Entrer votre choix : '))
        except:
            print('Wrong input. Please enter a number ...')
        if option == 1:
          controller = PlayerController()
          controller.add_player()
          #print("Add player")
        elif option == 2:
          controller = TournoisController()
          controller.add_tournament()

            #print("List_of_player player")
        elif option == 3:
          controller = TournoisController()
          controller.add_tournament()
            #Add_Tournament()
            print("Add_Tournament")
        elif option == 4:
            #List_of_Tournament()
            print("List_of_Tournament")
        elif option == 5:
          print('C\'est fini ! ')
          exit()
        else:
          print('Choix Invalide.S\'il vous plait entrer un nombre entre 1 et 4.')

if __name__ == 'main':
    main()
  # exécute la fonction main (l5) a partir du moment ou dans la console on lance le script pyhton main.py 